---
phase: 04-emulator-integration-and-launch
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified: []
autonomous: false
requirements: [EMU-01, EMU-02]
must_haves:
  truths:
    - "All Phase 4 tests pass"
    - "Full test suite passes (no regressions)"
    - "User can configure an emulator in Settings and launch a game via Play button"
  artifacts: []
  key_links: []
---

<objective>
Run full test suite, verify no regressions, and human-verify the end-to-end emulator configuration and launch flow.

Purpose: Final gate before marking Phase 4 complete. Ensures all automated tests pass and the user can verify the visual/interactive aspects of the launch overlay and settings UI.
Output: Verification that Phase 4 is ready for `/gsd:verify-work`.
</objective>

<execution_context>
@C:/Users/remyg/Documents/Projet/YARL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/remyg/Documents/Projet/YARL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-emulator-integration-and-launch/04-CONTEXT.md
@.planning/phases/04-emulator-integration-and-launch/04-03-SUMMARY.md
@.planning/phases/04-emulator-integration-and-launch/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any regressions</name>
  <files></files>
  <read_first>
    - .planning/phases/04-emulator-integration-and-launch/04-VALIDATION.md (sampling rate and test commands)
  </read_first>
  <action>
1. Run Phase 4 tests: `dotnet test tests/YARL.Tests/ --filter "Category=Phase4" --no-build`
2. Run full test suite: `dotnet test tests/YARL.Tests/ --no-build`
3. If any tests fail, diagnose and fix. Common issues:
   - Constructor signature changes in MainViewModel, SettingsViewModel, or GameDetailViewModel may break existing tests that construct these VMs
   - Check all test files that instantiate these VMs and update constructor calls
4. Re-run until all green.
  </action>
  <verify>
    <automated>cd C:/Users/remyg/Documents/Projet/YARL && dotnet test tests/YARL.Tests/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test tests/YARL.Tests/` exits with code 0
    - Output contains "Passed!" with 0 failed tests
  </acceptance_criteria>
  <done>All automated tests pass including Phase 4 tests and full regression suite</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify end-to-end emulator configuration and launch flow</name>
  <files></files>
  <action>
Present the verification steps to the user and wait for approval. The user will test the full emulator integration flow manually.
  </action>
  <verify>User confirms "approved"</verify>
  <done>User has verified the Settings EMULATORS section, Play button, launch overlay, and auto-dismiss behavior</done>
  <what-built>
    Complete emulator integration: Settings EMULATORS section for configuring emulators per platform, Play button in game detail drawer wired to GameLaunchService, full-page launch overlay with Launching/Running/Failed states, live play-time counter, Switch to Game and Dismiss buttons.
  </what-built>
  <how-to-verify>
    1. Run the app: `dotnet run --project src/YARL/`
    2. Go to Settings (gear icon in sidebar)
    3. Scroll down to the EMULATORS section — verify you see rows for platforms that have games in your library
    4. For one platform (e.g. SNES), enter the path to an emulator executable and click Save
    5. Verify the green checkmark appears next to the path (path validity)
    6. Click "+ Add platform" and verify a dropdown of unconfigured platforms appears
    7. Go back to the game library, select a game for the configured platform
    8. In the game detail drawer, verify the Play button is visible (not the "no emulator configured" prompt)
    9. Click Play — verify the launch overlay appears with the game title and cover art background
    10. If the emulator launches: verify the overlay transitions to Running state with live timer
    11. Click "Switch to Game" — verify the emulator window comes to focus (Windows only)
    12. Click "Dismiss" — verify the overlay closes but the emulator keeps running
    13. Close the emulator — verify the overlay auto-dismisses (if still visible)
    14. For a platform WITHOUT an emulator configured, verify the detail drawer shows "No emulator configured for [platform]" with a "Configure in Settings" button
    15. If on Windows: verify the Flatpak checkbox is greyed out (disabled)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All Phase 4 automated tests pass
- Full test suite passes (no regressions from Phase 1-3 work)
- Human verified: Settings emulator config, Play button, launch overlay, auto-dismiss
</verification>

<success_criteria>
Phase 4 is complete: users can configure emulators in Settings and launch games via the Play button with a full-page overlay showing launch status, live play time, and control buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emulator-integration-and-launch/04-05-SUMMARY.md`
</output>
