---
phase: 04-emulator-integration-and-launch
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/YARL.Tests/Phase4/EmulatorConfigTests.cs
  - tests/YARL.Tests/Phase4/EmulatorRowViewModelTests.cs
  - tests/YARL.Tests/Phase4/GameLaunchServiceTests.cs
  - tests/YARL.Tests/Phase4/LaunchOverlayViewModelTests.cs
autonomous: true
requirements: [EMU-01, EMU-02]
must_haves:
  truths:
    - "Phase 4 test stub files exist with [Trait(Category, Phase4)] and compile"
    - "Each stub has at least one [Fact] method that fails with Assert.Fail"
  artifacts:
    - path: "tests/YARL.Tests/Phase4/EmulatorConfigTests.cs"
      provides: "EMU-01 config persistence test stubs"
      contains: "[Trait(\"Category\", \"Phase4\")]"
    - path: "tests/YARL.Tests/Phase4/EmulatorRowViewModelTests.cs"
      provides: "EMU-01 path validity and Flatpak checkbox test stubs"
      contains: "[Trait(\"Category\", \"Phase4\")]"
    - path: "tests/YARL.Tests/Phase4/GameLaunchServiceTests.cs"
      provides: "EMU-02 launch arg building and DB update test stubs"
      contains: "[Trait(\"Category\", \"Phase4\")]"
    - path: "tests/YARL.Tests/Phase4/LaunchOverlayViewModelTests.cs"
      provides: "EMU-02 overlay state machine test stubs"
      contains: "[Trait(\"Category\", \"Phase4\")]"
  key_links: []
---

<objective>
Create Wave 0 test stub files for Phase 4 so that all subsequent implementation plans have failing tests to drive development.

Purpose: VALIDATION.md mandates test stubs exist before any implementation. These stubs define the behavioral contracts that Plans 02-05 will make pass.
Output: Four test files in tests/YARL.Tests/Phase4/ with failing [Fact] stubs.
</objective>

<execution_context>
@C:/Users/remyg/Documents/Projet/YARL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/remyg/Documents/Projet/YARL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-emulator-integration-and-launch/04-CONTEXT.md
@.planning/phases/04-emulator-integration-and-launch/04-RESEARCH.md
@.planning/phases/04-emulator-integration-and-launch/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EMU-01 test stubs (EmulatorConfigTests + EmulatorRowViewModelTests)</name>
  <files>tests/YARL.Tests/Phase4/EmulatorConfigTests.cs, tests/YARL.Tests/Phase4/EmulatorRowViewModelTests.cs</files>
  <read_first>
    - tests/YARL.Tests/Phase3/ScraperPipelineTests.cs (existing test pattern: namespace, usings, Trait attribute, NSubstitute usage)
    - src/YARL/Infrastructure/Config/AppConfig.cs (current AppConfig shape — stubs will reference EmulatorConfig which does not exist yet)
    - src/YARL/Infrastructure/Config/AppConfigService.cs (Load/Save methods for JSON round-trip test)
  </read_first>
  <action>
Create `tests/YARL.Tests/Phase4/EmulatorConfigTests.cs`:
- Namespace: `YARL.Tests.Phase4`
- Class attribute: `[Trait("Category", "Phase4")]`
- Stub methods (all with `Assert.Fail("Not implemented — Wave 0 stub")`):
  1. `EmulatorConfig_RoundTrip_JsonSerializeDeserialize` — will test that `AppConfig` with `EmulatorConfigs` dictionary survives JSON save/load
  2. `EmulatorConfig_DefaultArgs_IsRompath` — will test that `new EmulatorConfig().Args` equals `"{rompath}"`
  3. `EmulatorConfig_EmptyDictionary_DeserializesToEmpty` — will test that loading config with no emulators key gives empty dict

Create `tests/YARL.Tests/Phase4/EmulatorRowViewModelTests.cs`:
- Namespace: `YARL.Tests.Phase4`
- Class attribute: `[Trait("Category", "Phase4")]`
- Stub methods (all with `Assert.Fail("Not implemented — Wave 0 stub")`):
  1. `PathValidity_ExistingFile_ShowsValid` — will test IsPathValid computed property
  2. `PathValidity_NonExistentFile_ShowsInvalid`
  3. `IsFlatpakAvailable_OnWindows_ReturnsFalse` — will test RuntimeInformation guard
  4. `FlatpakChecked_HidesFileBrowse` — will test that IsFlatpak=true implies browse hidden
  5. `SaveCommand_PersistsToAppConfig` — will test per-row save flow

Note: These files will NOT compile until Plan 02 creates the `EmulatorConfig` record and `EmulatorRowViewModel` class. That is expected — Wave 0 stubs define contracts. Add `// TODO: uncomment when EmulatorConfig exists` comments around the using directives that will be needed, and use `#if false` / `#endif` preprocessor blocks around the test class bodies so the project compiles. Each file should have a single compilable `[Fact]` outside the `#if false` block:
```csharp
[Fact]
[Trait("Category", "Phase4")]
public void Stub_FailsUntilImplemented() => Assert.Fail("Not implemented - Wave 0 stub");
```
  </action>
  <verify>
    <automated>cd C:/Users/remyg/Documents/Projet/YARL && dotnet build tests/YARL.Tests/ --no-restore 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/YARL.Tests/Phase4/EmulatorConfigTests.cs exists and contains `[Trait("Category", "Phase4")]`
    - tests/YARL.Tests/Phase4/EmulatorRowViewModelTests.cs exists and contains `[Trait("Category", "Phase4")]`
    - Both files contain `Assert.Fail("Not implemented` in at least one [Fact] method
    - `dotnet build tests/YARL.Tests/` succeeds (exit code 0)
  </acceptance_criteria>
  <done>Two EMU-01 test stub files exist, compile, and contain failing [Fact] stubs</done>
</task>

<task type="auto">
  <name>Task 2: Create EMU-02 test stubs (GameLaunchServiceTests + LaunchOverlayViewModelTests)</name>
  <files>tests/YARL.Tests/Phase4/GameLaunchServiceTests.cs, tests/YARL.Tests/Phase4/LaunchOverlayViewModelTests.cs</files>
  <read_first>
    - tests/YARL.Tests/Phase3/ScraperPipelineTests.cs (existing test pattern)
    - .planning/phases/04-emulator-integration-and-launch/04-VALIDATION.md (test map for EMU-02 behaviors)
  </read_first>
  <action>
Create `tests/YARL.Tests/Phase4/GameLaunchServiceTests.cs`:
- Namespace: `YARL.Tests.Phase4`
- Class attribute: `[Trait("Category", "Phase4")]`
- Stub methods (all with `Assert.Fail("Not implemented — Wave 0 stub")`):
  1. `BuildStartInfo_NativeEmulator_SetsFileNameAndArgs` — will test native ProcessStartInfo construction
  2. `BuildStartInfo_Flatpak_SetsFileNameToFlatpakAndRunArgs` — will test Flatpak PSI construction
  3. `BuildStartInfo_RomPathWithSpaces_IsQuoted` — will test `{rompath}` substitution with quoting
  4. `OnProcessExited_UpdatesLastPlayedAtAndTotalPlayTime` — will test DB update after exit (in-memory SQLite)
  5. `OnProcessExited_NonZeroExitCodeWithinGrace_SetsFailed` — will test failed state detection

Create `tests/YARL.Tests/Phase4/LaunchOverlayViewModelTests.cs`:
- Namespace: `YARL.Tests.Phase4`
- Class attribute: `[Trait("Category", "Phase4")]`
- Stub methods (all with `Assert.Fail("Not implemented — Wave 0 stub")`):
  1. `InitialState_IsHidden` — will test default state
  2. `StartLaunch_TransitionsToLaunching` — will test Hidden -> Launching
  3. `ProcessStarted_TransitionsToRunning` — will test Launching -> Running
  4. `ProcessExited_TransitionsToHidden` — will test Running -> Hidden (auto-dismiss)
  5. `ProcessFailed_TransitionsToFailed` — will test error state
  6. `DismissCommand_SetsHidden_DoesNotKillProcess` — will test dismiss behavior

Same approach as Task 1: use `#if false` / `#endif` around class bodies that reference unimplemented types. Include one compilable `[Fact]` stub per file:
```csharp
[Fact]
[Trait("Category", "Phase4")]
public void Stub_FailsUntilImplemented() => Assert.Fail("Not implemented - Wave 0 stub");
```
  </action>
  <verify>
    <automated>cd C:/Users/remyg/Documents/Projet/YARL && dotnet test tests/YARL.Tests/ --filter "Category=Phase4" --no-restore 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/YARL.Tests/Phase4/GameLaunchServiceTests.cs exists and contains `[Trait("Category", "Phase4")]`
    - tests/YARL.Tests/Phase4/LaunchOverlayViewModelTests.cs exists and contains `[Trait("Category", "Phase4")]`
    - Both files contain `Assert.Fail("Not implemented` in at least one [Fact] method
    - `dotnet test --filter "Category=Phase4"` runs and reports 4 failed tests (one per file)
  </acceptance_criteria>
  <done>Four Phase 4 test stub files exist, compile, and report failing tests when run with Category=Phase4 filter</done>
</task>

</tasks>

<verification>
- `dotnet build tests/YARL.Tests/` succeeds
- `dotnet test tests/YARL.Tests/ --filter "Category=Phase4" --no-build` reports exactly 4 test failures (one per stub file)
- All four files in tests/YARL.Tests/Phase4/ directory
</verification>

<success_criteria>
Four test stub files created in tests/YARL.Tests/Phase4/ with [Trait("Category", "Phase4")] attributes, compilable stubs, and failing Assert.Fail tests that define the behavioral contracts for Plans 02-05.
</success_criteria>

<output>
After completion, create `.planning/phases/04-emulator-integration-and-launch/04-01-SUMMARY.md`
</output>
